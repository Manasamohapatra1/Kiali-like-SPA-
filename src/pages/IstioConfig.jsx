import { Box, Typography } from "@mui/material";

const IstioConfig = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h4">Istio Config</Typography>
    <Typography>
      Display mock Istio config or service-related configs here.
    </Typography>
  </Box>
);

export default IstioConfig;
